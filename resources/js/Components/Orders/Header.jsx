const Header = ({ title }) => {
    return (
      <div style={{ padding: "20px", fontSize: "1.5rem", fontWeight: "bold", color: "#fff" }}>
        {title}
      </div>
    );
  };
  
  export default Header;
  